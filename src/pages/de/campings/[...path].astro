---
import CampingPathPage from '../../../components/CampingPathPage.astro';
import { campingStaticPaths } from '../../../lib/routes';

export function getStaticPaths() {
  return campingStaticPaths();
}

const locale = 'de' as const;
---

<CampingPathPage locale={locale} path={Astro.props} />
